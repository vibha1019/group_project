---
toc: false
comments: true
layout: post
title: Group Project
description: PBL Unit 1 / Week 3 plan using Utterance
type: plans
courses: { compsci: {week: 3} }
permalink: /plans/week3
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            margin: 0;
            overflow: hidden;
            background-color: #87CEEB; /* Sky Blue */
        }
        #hamster {
            position: absolute;
            bottom: 10px;
            left: 0;
            transform: translateX(0);
            font-size: 10em; /* Adjust the font size to make the hamster bigger */
            transition: transform 0.3s ease-out; /* Adjust the transition duration for a faster slide */
        }
        #score {
            position: absolute;
            bottom: 10px;
            right: 10px;
            font-size: 2em;
            color: white;
        }
        .carrot {
            position: absolute;
            font-size: 4em; /* Adjust the font size to make the carrots bigger */
            animation: fall 1s linear infinite;
        }
        @keyframes fall {
            to {
                transform: translateY(80vh);
            }
        }
    </style>
</head>
<body>
    <div id="hamster">
        🐹
    </div>
    <div id="score">Score: 0</div>
    <script>
        const hamster = document.getElementById('hamster');
        const scoreElement = document.getElementById('score');
        let hamsterPosition = 0;
        let score = 0;
        window.addEventListener('keydown', (event) => {
            if (event.key === 'ArrowLeft') {
                moveHamster('left');
            } else if (event.key === 'ArrowRight') {
                moveHamster('right');
            }
        });
        function moveHamster(direction) {
            const step = 45; // Adjust the step for a faster slide
            const bodyWidth = document.body.clientWidth;
            if (direction === 'left') {
                hamsterPosition = Math.max(hamsterPosition - step, 0);
            } else if (direction === 'right') {
                hamsterPosition = Math.min(hamsterPosition + step, bodyWidth);
            }
            hamster.style.transform = `translateX(${hamsterPosition}px)`;
        }
        document.addEventListener('DOMContentLoaded', function () {
    // Function to create a new carrot element
    function createCarrot() {
        const carrot = document.createElement('div');
        carrot.className = 'carrot';
        carrot.style.left = `${Math.random() * 90 + 5}vw`;
        document.getElementById('gameContainer').appendChild(carrot);
        return carrot;
    }
            carrot.addEventListener('animationend', () => {
                carrot.remove();
            });
            checkCollision(carrot);
        }
    // Function to set random position for a carrot
    function setRandomPosition(carrot) {
        const randomX = Math.floor(Math.random() * window.innerWidth);
        const randomY = -30; // Starts above the top of the screen
        carrot.style.left = `${randomX}px`;
        carrot.style.top = `${randomY}px`;
    }
    // Function to make a carrot fall
    function fall(carrot) {
        const speed = Math.floor(Math.random() * 5) + 3; // Random speed between 3 and 7
        const interval = setInterval(function () {
            const currentTop = parseInt(carrot.style.top, 10);
            const newTop = currentTop + speed;
            carrot.style.top = `${newTop}px`;
            // Check if the carrot has reached the bottom of the screen
            if (newTop > window.innerHeight) {
                clearInterval(interval);
                setRandomPosition(carrot); // Reset position for the next fall
                fall(carrot); // Start falling again
            }
        }, 30); // Update every 30 milliseconds
    }
    // Create and start falling for multiple carrots
    const numberOfCarrots = 10;
    for (let i = 0; i < numberOfCarrots; i++) {
        const carrot = createCarrot();
        setRandomPosition(carrot);
        fall(carrot);
    });
</body>
</html>
